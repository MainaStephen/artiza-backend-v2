# from django.urls import path
# from .views import (
#     ArtisanMatchListView,
#     MarkMatchViewedView,
#     RespondToMatchView,
#     ProjectMatchesListView
# )

urlpatterns = [
    # Artisan endpoints
    # path("artisan-matches/", ArtisanMatchListView.as_view(), name="artisan-matches"),
    # path("matches/<int:match_id>/view/", MarkMatchViewedView.as_view(), name="mark-viewed"),
    # path("matches/<int:match_id>/respond/", RespondToMatchView.as_view(), name="respond-match"),
    
    # # Client endpoint - get matches for a specific project
    # path("project/<int:project_id>/matches/", ProjectMatchesListView.as_view(), name="project-matches"),
]